/**
 * Rank configuration (display + ordering only).
 * Mechanics (caps, scaling, etc.) come later.
 */

export const RANKS = {
  normal: "Normal",
  iron: "Iron",
  bronze: "Bronze",
  silver: "Silver",
  gold: "Gold",
  diamond: "Diamond"
};

/**
 * Optional: canonical ordering for dropdowns / progression.
 * Use this to iterate ranks in a predictable order.
 */
export const RANK_ORDER = ["normal", "iron", "bronze", "silver", "gold", "diamond"];
